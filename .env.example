# Copy to .env (gitignored) if you want to export third-party model provider
# keys into the shell that starts the backend, instead of filling them into
# agentlane.yaml's model_providers.<name>.api_key. Name each var to match
# the api_key_env you configured for that provider, e.g.:
OPENROUTER_API_KEY=sk-or-v1-xxxx
